# Run the project

1. Install dependencies:

```bash
npm i
```

2. Create a `.env.local` file in the root directory and add your environment variables (like MongoDB connection URI).

3. Start the development server:

```bash
npm run dev
```

## 🛠️ Tech Stack
- Next.js (App Router)
- MongoDB

